# ADR-006 — <the decision, as a short noun phrase>

<!--
PA7 architecture decision record. Required: a public test checks that the
five `##` headings below are present, and that the last one lists at least
three items. The manual 30% of this assignment is the quality of what you
write under them.

Keep the headings exactly as they are. One page is enough.

The question this ADR is really asking you:

    You built the same saga twice. Which lines of your PA6 orchestrator no
    longer exist in PA7, what did the engine take over in their place — and
    what did you have to start running, watching and paying for in return?

Write it with your PA6 orchestrator open next to your workflow.
-->

**Status:** accepted
**Date:** YYYY-MM-DD

## Context

<!--
What forced a decision? Be concrete. Kill your PA6 orchestrator mid-saga
(docker kill pa6-orchestrator while shipping is slow) and say what it left
behind: which record, in which state, and what the next request with that
key got. That is the problem this assignment exists to solve.

Worth naming if you hit them: deciding which failures Temporal may retry
(a declined payment against a service that did not answer), the shipping
call that ran twice in the crash test, and anything the workflow sandbox
refused that your PA6 code did freely.
-->

## Decision

<!--
What you built, specifically enough that I could find it in your code:
where the boundary between workflow and activities sits, your retry policy
and why those numbers, how an activity decides that a failure is permanent,
and how compensation is expressed in the workflow.
-->

## Alternatives considered

<!--
At least one real alternative with the reason you rejected it. Real options
here include: keeping the hand-rolled PA6 orchestrator and adding a
recovery job that finishes stuck sagas on startup; retrying every failure
and letting compensation sort it out; one activity per saga instead of one
per call; a BPMN engine (Camunda, Flowable) instead of workflow-as-code.

The first one is not a straw man. Say what it would take.
-->

## Consequences

<!--
What is now easier, what is now harder, and what breaks first.

Concretely: the crash test called shipping twice. Why can no engine make
that exactly once, and what would it mean for authorizePayment if it were
the step that ran twice? What happens to workflows that are running when
you deploy a changed workflow? What do you now have to operate that PA6
did not need? Name a cost you accepted — a consequences section with no
downside in it is not credible.
-->

## What the engine took over

<!--
A list, at least three items. For each: the responsibility, where your PA6
orchestrator did it (function or line), and what does it in PA7 instead.
For example (do not copy it — it is one of the easy ones):

- Timeouts: PA6 wrapped every axios call in its own timeout and turned
  ECONNABORTED into "timeout" by hand (callDownstream). In PA7 the
  activity's startToCloseTimeout does it, and the workflow sees a
  TimeoutFailure.
-->
