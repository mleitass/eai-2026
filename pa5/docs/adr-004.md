# ADR-004 — <the decision, as a short noun phrase>

<!--
PA5 architecture decision record. Required: a public test checks that the
four `##` headings below are present, and the manual 30% of this
assignment is the quality of what you write under them.

Keep the headings exactly as they are. One page is enough.

You made at least five real decisions building this week's messaging
layer. Pick the ones that were genuinely open questions for you rather than
walking through all of them:

  - Where correlationId actually lives on the wire, and why there instead
    of AMQP's own `properties.correlationId`.
  - What your retry threshold is (this course's scaffold uses 3 attempts /
    1s TTL) and what happens to a legitimately slow-but-healthy downstream
    if that threshold is too tight.
  - Why notification-service's idempotency check is a file
    (`/data/processed-ids.json`) and not an in-memory `Set` — what would
    actually go wrong with the in-memory version, concretely, not just "it's
    not durable".
  - What you classify as permanent, beyond a body that is not valid JSON,
    and what it would cost to get that classification wrong in either
    direction.
  - What makes your replay safe: what it resets, where it routes, and what
    happens if it runs twice, or while the cause of the failure is still
    there.
  - If you touched the retry topology, the fan-out-on-retry vs.
    return-to-own-queue trade-off it embodies.

Write it after you have finished the code, while the annoyance is fresh.
-->

**Status:** accepted
**Date:** YYYY-MM-DD

## Context

<!--
What did this week's topology actually make you decide, concretely? Not
"messaging is hard" — name the header, the queue, the retry count, the file.

Worth naming if you hit them: the moment you realized `properties.headers`
and `properties.correlationId` are two different, easily-confused things;
watching a message you already logged arrive at notification-service a
second time and having to explain why; the first time your retry count
came back wrong because of an off-by-one against MAX_RETRIES.
-->

## Decision

<!--
What you built, specifically enough that I could find it in your code. Name
the function and what it does. Include the decisions that are easy to skip
over: exactly which AMQP property carries correlationId and why, what
"processed" means for notification-service and where it is persisted, and
what your retry/DLQ code does differently (if anything) from the
payment-service scaffold you copied it from.
-->

## Alternatives considered

<!--
At least one real alternative with the reason you rejected it. Real options
here include: AMQP's native `properties.correlationId` instead of a custom
header; an in-memory idempotency Set instead of a file; requeue=true
instead of the DLX-based retry path; a shorter or longer retry TTL/count
than the scaffold's 1s/3; keeping the naive fan-out-on-retry topology
(every consumer sees every retried message) instead of the
per-queue-return-routing this scaffold ships with; one "errors" queue
instead of separate dead letter and invalid message channels; declaring
those channels in definitions.json instead of in each service's code;
replaying through orders.exchange instead of orders.return.exchange.
-->

## Consequences

<!--
What is now easier, what is now harder, and what breaks first.

Concretely: what happens to correlation tracing if a future consumer
forwards a message without carrying the header along? What happens to
notification-service's dedup file if two replicas of it run at once? What
happens to an order if payment succeeds but inventory is down for longer
than three retry cycles — does anything downstream ever find out? Name a
cost you accepted — a consequences section with no downside in it is not
credible.
-->
