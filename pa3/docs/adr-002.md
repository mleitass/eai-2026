# ADR-002 — <the decision, as a short noun phrase>

<!--
PA3 architecture decision record.

Copy this file to your own repo at pa3/docs/adr-002.md and fill it in.
Keep the four `##` headings exactly as they are -- a public test checks
that they are present, and it checks the headings, not the prose.

This document carries a real part of your mark. The manual 30% of this
assignment is ADR quality and the honesty of your self-assessment. One
page is enough; three pages of restated requirements is not better.

This assignment forces four decisions that do not have one obviously
correct answer. Your ADR should show your reasoning on at least the ones
you actually had to think about -- you do not need to write a paragraph on
every bullet if one of them was truly a non-issue for you, but say so
rather than skipping it silently:

  - How you preserve correlation once the splitter turns one order into N
    independent messages travelling independent paths. What field(s) carry
    it, and what would break if a worker stripped or renamed one?
  - What timeout value you would run in production, and why. The
    assignment fixes the mechanism (an idle timeout that resets on each
    result received) and the test value (AGGREGATOR_IDLE_TIMEOUT_SECONDS=5
    in docker-compose.yml); the open decision is the number. Too short and
    normal processing latency (a slow physical item) trips a false
    partial; too long and "partial" stops being useful. What would you
    measure to choose it -- and why would a fixed deadline from the
    order's start have been wrong once an order has 50 items instead of 3?
  - What a "partial" result actually contains. Is it just "not complete"
    or does it tell the caller exactly which items are missing, and why
    that distinction matters to whoever consumes orders.complete.
  - How you handle a duplicate item result (the same item redelivered --
    this happens for real whenever a worker acks late and RabbitMQ
    redelivers). What in your data structure makes a duplicate a no-op
    instead of a second entry?

Write it after you have finished the code, while the annoyance is fresh.
-->

**Status:** accepted
**Date:** YYYY-MM-DD

## Context

<!--
What forced a decision? The constraint, not the assignment text. What did
you not know when you started? If nothing was genuinely open, say so and
say what the obvious path was.
-->

## Decision

<!--
What you did, in the active voice, specifically enough that someone could
find it in your code. "The aggregator resets last_seen on every message in
aggregate_result, and a background thread sweeps in_flight every second
for orders idle past IDLE_TIMEOUT_SECONDS" -- not "we chose good timeout
handling".
-->

## Alternatives considered

<!--
At least one real alternative, with the reason you rejected it. An
alternative you can dismiss in three words was not a real alternative,
and this is the section that most often separates a good ADR from a
filled-in form. Real options here include: a fixed deadline from order
creation instead of an idle timeout; storing aggregation state in Redis
instead of an in-memory dict; deduplicating by re-declaring the queue with
a different acknowledgment mode instead of tracking itemIndex yourself;
"None" is an acceptable answer only if you can defend it.
-->

## Consequences

<!--
What is now easier, what is now harder, and what you would hit first if
this system had ten times the traffic, or an order with 500 items instead
of 5. Name a cost you accepted -- what happens to your in-memory
aggregation state if the aggregator container restarts mid-order? A
consequences section with no downside in it is not credible.
-->
