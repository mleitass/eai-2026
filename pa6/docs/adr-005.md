# ADR-005 — <the decision, as a short noun phrase>

<!--
PA6 architecture decision record. Required: a public test checks that the
four `##` headings below are present, and the manual 30% of this assignment
is the quality of what you write under them.

Keep the headings exactly as they are. One page is enough.

The question this ADR is really asking you:

    You wrote the contract before the code, then built a saga with
    compensation on top of four services you do not control. Where did the
    contract-first discipline actually change what you built — and where did
    the saga's rollback logic get genuinely hard, specifically because two of
    the four downstream services (shipping, notification) have no
    compensating action at all?

Write it after you have finished the code, while the annoyance is fresh.
-->

**Status:** accepted
**Date:** YYYY-MM-DD

## Context

<!--
What forced a decision? Be specific and concrete — name the step, the HTTP
code, the race you had to close. "Idempotency was tricky" is not context;
"a second request arriving while the first was still mid-saga would read an
`in_progress` idempotency record and needed its own response, not a silent
hang" is.

Worth naming if you hit them: the shipping/notification steps having no
compensating endpoint, choosing what counts as a "timeout" versus a business
failure from axios, deciding what `status` a saga reports when compensation
itself fails, and anything the OpenAPI lint rules forced you to make
explicit that you would otherwise have left vague.
-->

## Decision

<!--
What you built, specifically enough that I could find it in your code. Name
the function and what it does. Include the decisions that are easy to skip
over: your compensation ordering, how a downstream timeout is distinguished
from a downstream business failure, and what a replay actually returns.
-->

## Alternatives considered

<!--
At least one real alternative with the reason you rejected it. Real options
here include: parallelizing independent downstream calls instead of strict
sequencing; a separate "pending" HTTP status instead of always returning
final state synchronously; storing the OpenAPI schema as the single source
of truth and generating request validation from it instead of hand-written
checks; giving shipping/notification their own compensating endpoints on the
mock services instead of accepting that some steps cannot be rolled back.

That last one is not a joke, and it is the alternative most worth writing up.

You have also built the other answer already: in PA5 the same canonical
order went out as an event and each service reacted on its own
(choreography). Which of the two fits checkout, and what would you lose
with the other?
-->

## Consequences

<!--
What is now easier, what is now harder, and what breaks first.

Concretely: what happens to your code when a fifth downstream step is added
to the saga? When two requests for the same orderId (different idempotency
keys) race each other? When REQUEST_TIMEOUT_MS is set lower than the
mock services' own healthcheck interval? When the orchestrator is killed
mid-saga, leaving an `in_progress` record behind — what does the next
request with that key get, forever? Name a cost you accepted — a
consequences section with no downside in it is not credible.
-->
